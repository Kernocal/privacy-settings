name: Release Current Version
on:
  workflow_dispatch:
    inputs:
      dryRun:
        default: false
        type: boolean
        description: Dry run

jobs:
  validate:
    name: Validate
    uses: ./.github/workflows/validate.yml
    secrets: inherit

  submit:
    name: Submit
    runs-on: ubuntu-24.04
    needs: [validate]
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Configure Git
        run: |
          git config user.email "github-actions@users.noreply.github.com"
          git config user.name "GitHub Actions"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Get Version
        id: version
        run: |
          NEW_VERSION=$(node -p "require('./package.json').version")
          pnpm dlx changelogen@latest --release "$NEW_VERSION" --output CHANGELOG.md --no-bump
          echo "newVersion=$NEW_VERSION" >> "$GITHUB_OUTPUT"

      - name: Build and Zip
        run: |
          pnpm run zip
          pnpm run zip-edge

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          path: .output/*.zip
          if-no-files-found: error
          include-hidden-files: true

      - name: Commit and Push
        if: ${{ !inputs.dryRun }}
        run: |
          git add --force CHANGELOG.md
          git commit -am "chore(release): v$NEW_VERSION"
          git tag v$NEW_VERSION
          git push
          git push --tags
        env:
          NEW_VERSION: ${{ steps.version.outputs.newVersion }}

      - name: Release
        if: ${{ !inputs.dryRun }}
        run: gh release create v${{ steps.version.outputs.newVersion }} .output/*.zip --title "v${{ steps.version.outputs.newVersion }}" --notes-file CHANGELOG.md
        env:
          GH_TOKEN: ${{ github.token }}